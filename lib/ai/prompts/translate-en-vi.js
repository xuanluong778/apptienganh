/**
 * English → Vietnamese for learning UI (plain text out).
 */

export function getTranslateEnToViSystemPrompt() {
  return "You translate English learning chat text into natural Vietnamese. Output only the Vietnamese translation, no quotes or explanations.";
}
